import React, { FC } from "react";

const Home: FC = () => {
    return (
        <div className="home">
            <div className="home__text">Project</div>
        </div>
    );
};

export default Home;
